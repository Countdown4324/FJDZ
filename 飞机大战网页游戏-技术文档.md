# 星际战机 · 飞机大战网页游戏 — 技术文档

> 版本：v1.0  
> 适用对象：独立开发者（Solo Builder）  
> 技术栈：HTML5 + Canvas + JavaScript（纯前端，无需后端）

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈与工具](#2-技术栈与工具)
3. [游戏核心玩法设计](#3-游戏核心玩法设计)
4. [游戏架构设计](#4-游戏架构设计)
5. [模块详细设计](#5-模块详细设计)
6. [UI/UX 与视觉风格](#6-uiux-与视觉风格)
7. [数据与状态管理](#7-数据与状态管理)
8. [资源清单](#8-资源清单)
9. [开发路线图](#9-开发路线图)
10. [附录：参考与灵感来源](#10-附录参考与灵感来源)

---

## 1. 项目概述

### 1.1 游戏简介

《星际战机》是一款基于 HTML5 Canvas 的竖版飞行射击游戏（Shoot 'em up）。玩家控制一艘星际战机，在无尽的太空中迎战一波波敌人，收集强化道具，最终挑战强大的 Boss。

### 1.2 核心特色

| 特性 | 说明 |
|------|------|
| **多战机选择** | 3 种不同属性的战机（均衡型 / 速射型 / 重火力型） |
| **丰富敌人体系** | 6+ 种普通敌人 + 精英怪 + 关底 Boss |
| **强力道具系统** | 武器升级、护盾、僚机、全屏炸弹、生命恢复 |
| **连击评分系统** | 连续击杀产生连击倍率，高分挑战 |
| **科技感视觉** | 霓虹光效、粒子爆炸、动态星空背景、光晕特效 |
| **多端适配** | 桌面端（键盘）+ 移动端（触屏）双支持 |
| **无尽模式 + Boss Rush** | 两种核心玩法模式 |

### 1.3 目标用户

- 核心玩法：休闲玩家、飞行射击爱好者
- 年龄层：12-40 岁
- 平台：PC 浏览器 + 移动端浏览器

---

## 2. 技术栈与工具

### 2.1 技术选型

```
前端框架：无框架（原生 JavaScript）
渲染引擎：HTML5 Canvas 2D
样式方案：原生 CSS3（Flexbox + Grid + Animation）
构建工具：无（单页应用，直接运行）
代码规范：ES6+ 标准
```

### 2.2 为什么选择纯原生

| 方案 | 优势 | 劣势 |
|------|------|------|
| 原生 Canvas | 零依赖、性能极致、控制粒度细 | 开发量稍大 |
| Phaser.js | 内置物理引擎、成熟 | 包体积大、学习成本 |
| PixiJS | 高性能渲染 | 过度设计，小游戏不需要 |

**结论：** 纯原生 Canvas 最适合独立开发者快速构建小型射击游戏。

### 2.3 目录结构

```
airplane-war/
├── index.html            # 入口页面
├── css/
│   ├── style.css         # 全局样式
│   └── animations.css    # 关键帧动画
├── js/
│   ├── main.js           # 游戏入口 & 初始化
│   ├── Game.js           # 游戏主循环（核心引擎）
│   ├── Scene.js          # 场景管理器
│   ├── entities/
│   │   ├── Player.js     # 玩家战机
│   │   ├── Enemy.js      # 敌人基类
│   │   ├── Boss.js       # Boss 类
│   │   ├── Bullet.js     # 子弹类
│   │   └── PowerUp.js    # 道具类
│   ├── systems/
│   │   ├── Input.js      # 输入系统（键盘+触屏）
│   │   ├── Collision.js  # 碰撞检测系统
│   │   ├── Particle.js   # 粒子特效系统
│   │   ├── Wave.js       # 波次生成系统
│   │   └── Audio.js      # 音效系统
│   ├── ui/
│   │   ├── HUD.js        # 游戏内 HUD
│   │   ├── Menu.js       # 菜单界面
│   │   └── Score.js      # 分数/连击显示
│   └── utils/
│       ├── constants.js  # 游戏常量
│       ├── math.js       # 数学工具
│       └── storage.js    # 本地存储
├── assets/
│   ├── images/           # 图片资源
│   └── audio/            # 音频资源
└── README.md
```

---

## 3. 游戏核心玩法设计

### 3.1 游戏模式

| 模式 | 说明 |
|------|------|
| **无尽模式（Endless）** | 无限波次，难度递增，追求最高分 |
| **Boss Rush** | 连续挑战 Boss，每击败一个回复少量生命 |

### 3.2 玩家战机系统

#### 3.2.1 可选战机（3 种）

| 战机 | 属性 | 初始武器 | 特殊技能 |
|------|------|----------|----------|
| **先锋号** | 均衡型：HP 5, 攻速 8/s, 移速 5 | 双发子弹 | 无（均衡） |
| **速射号** | 敏捷型：HP 3, 攻速 12/s, 移速 7 | 三发散射 | 闪现（CD 8s） |
| **毁灭号** | 重装型：HP 8, 攻速 4/s, 移速 3 | 单发加农 | 护盾（CD 12s） |

#### 3.2.2 武器升级系统

```
Lv.1 单发 → Lv.2 双发 → Lv.3 三发散射 → Lv.4 扇形弹幕 → Lv.5 追踪弹
```

- 每吃 1 个武器道具升级 1 级
- 被击中后降 1 级（最低 Lv.1）

### 3.3 敌人体系

#### 3.3.1 普通敌人

| 敌人 | 描述 | HP | 速度 | 攻击方式 |
|------|------|----|------|----------|
| 侦察兵 | 小型，直线飞行 | 1 | 快 | 不攻击 |
| 突击兵 | 中型，随机左右移动 | 2 | 中 | 单发子弹 |
| 狙击手 | 中型，悬停射击 | 2 | 慢 | 瞄准玩家射击 |
| 自爆兵 | 快速冲向玩家 | 1 | 很快 | 接触爆炸 |
| 护盾兵 | 前方有护盾 | 3 | 慢 | 间歇射击 |
| 精英怪 | 大型，特殊攻击模式 | 6 | 中 | 弹幕射击 |

#### 3.3.2 Boss 设计

**Boss 1 — 巨像级（第 5 波出现）**
- 血量：50
- 攻击模式：扇形弹幕 + 召唤小兵
- 阶段 2（血量 < 50%）：攻击频率翻倍 + 旋转激光

**Boss 2 — 毁灭级（第 10 波出现）**
- 血量：80
- 攻击模式：追踪弹 + 全屏弹幕
- 阶段 2（血量 < 50%）：释放旋转弹幕环

**Boss 3 — 最终BOSS（第 15 波出现）**
- 血量：120
- 攻击模式：3 种形态切换
  - 形态 1：扇形弹幕 + 激光扫射
  - 形态 2（HP < 66%）：追踪弹 + 分身
  - 形态 3（HP < 33%）：全屏弹幕 + 召唤大量小兵 + 旋转激光网

### 3.4 道具系统

| 道具 | 图标颜色 | 效果 |
|------|----------|------|
| 武器升级 (W) | 橙色 | 武器等级 +1 |
| 护盾 (S) | 蓝色 | 抵挡 1 次伤害 |
| 僚机 (F) | 绿色 | 增加 1 个自动射击的僚机（最多 2 个） |
| 全屏炸弹 (B) | 红色 | 清除屏幕上所有敌人和子弹 |
| 生命恢复 (H) | 粉色 | 恢复 1 点 HP |
| 分数加成 (★) | 金色 | 获得额外分数 |

### 3.5 评分系统

```
基础击杀得分 × 连击倍率 × 难度系数
```

- **连击系统：** 连续击杀敌人累积连击数，断连击后重置
  - 3-5 连击：×1.5
  - 6-10 连击：×2.0
  - 11-20 连击：×3.0
  - 20+ 连击：×5.0
- **断连击条件：** 超过 3 秒未击杀敌人 或 被击中
- **Boss 击杀奖励：** 基础分 × 当前连击倍率

---

## 4. 游戏架构设计

### 4.1 核心架构图

```
┌─────────────────────────────────────────────────┐
│                    Game.js                       │
│              (游戏主循环 / 核心引擎)               │
├─────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │  Scene   │  │  Input   │  │  Collision    │  │
│  │  Manager │  │  System  │  │  System       │  │
│  └──────────┘  └──────────┘  └───────────────┘  │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │  Wave    │  │ Particle │  │  Audio        │  │
│  │  System  │  │ System   │  │  System       │  │
│  └──────────┘  └──────────┘  └───────────────┘  │
├─────────────────────────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐  │
│  │Player│ │Enemy │ │ Boss │ │Bullet│ │Power │  │
│  │      │ │      │ │      │ │      │ │Up    │  │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘  │
├─────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │  HUD     │  │  Menu    │  │  Score        │  │
│  │  Display │  │  Screen  │  │  System       │  │
│  └──────────┘  └──────────┘  └───────────────┘  │
└─────────────────────────────────────────────────┘
```

### 4.2 游戏主循环（Game Loop）

使用 `requestAnimationFrame` 实现固定时间步长更新：

```
function gameLoop(timestamp) {
    const deltaTime = timestamp - lastTimestamp;
    lastTimestamp = timestamp;

    // 固定时间步长更新（60fps 基准）
    accumulator += deltaTime;
    while (accumulator >= STEP) {
        update(STEP);       // 更新逻辑
        accumulator -= STEP;
    }

    render();               // 渲染画面
    requestAnimationFrame(gameLoop);
}
```

### 4.3 实体生命周期

```
create() → init() → update(dt) → render(ctx) → destroy()
                                    ↓
                             碰撞检测回调
                            onCollision(entity)
```

所有实体继承自基类 `Entity`：

```javascript
class Entity {
    constructor(x, y, width, height) {
        this.x = x; this.y = y;
        this.width = width; this.height = height;
        this.active = true;  // false 时从场景移除
    }
    update(dt) { /* 子类重写 */ }
    render(ctx) { /* 子类重写 */ }
    destroy() { this.active = false; }
}
```

### 4.4 场景管理

```
SceneManager
├── MenuScene        # 主菜单
├── SelectScene      # 战机选择
├── GameScene        # 游戏主场景
├── PauseScene       # 暂停界面
└── GameOverScene    # 结算界面
```

每个场景独立管理自己的实体列表和更新逻辑。

---

## 5. 模块详细设计

### 5.1 Game.js — 核心引擎

**职责：**
- 管理游戏主循环（requestAnimationFrame）
- 初始化 Canvas、各个子系统
- 控制场景切换
- 管理全局状态（暂停、游戏结束等）

**关键接口：**

```javascript
class Game {
    constructor(canvasId)          // 初始化
    start()                        // 启动游戏循环
    switchScene(sceneName)         // 切换场景
    pause() / resume()             // 暂停/恢复
    gameOver(score)                // 游戏结束处理
}
```

### 5.2 Player.js — 玩家战机

**属性：**
- `x, y` — 位置（以画布底部为参考）
- `hp, maxHp` — 生命值
- `speed` — 移动速度
- `weaponLevel` — 武器等级（1-5）
- `skillCooldown` — 技能冷却
- `shield` — 护盾状态
- `wingmen[]` — 僚机列表

**方法：**
- `move(dx, dy)` — 移动（受边界限制）
- `shoot()` — 根据武器等级发射不同弹幕
- `useSkill()` — 使用特殊技能
- `takeDamage(amount)` — 受伤处理
- `upgradeWeapon()` — 武器升级

### 5.3 Enemy.js — 敌人系统

**敌人行为模式：**

```javascript
const ENEMY_PATTERNS = {
    LINEAR:     { move: (e) => e.y += e.speed },
    SINUSOIDAL: { move: (e) => { e.y += e.speed; e.x += Math.sin(e.y * 0.05) * 2 } },
    CHASE:      { move: (e) => { /* 追踪玩家 */ } },
    STATIONARY: { move: (e) => {}, shoot: (e) => { /* 瞄准射击 */ } }
};
```

### 5.4 Boss.js — Boss 系统

**阶段转换机制：**

```javascript
class Boss {
    update(dt) {
        // 根据 HP 百分比切换阶段
        if (this.hp < this.maxHp * 0.66) this.phase = 2;
        if (this.hp < this.maxHp * 0.33) this.phase = 3;

        // 当前阶段的行为
        this.patterns[this.phase].execute(this, dt);
    }
}
```

**Boss 攻击模式库：**
- `扇形弹幕` — 等角度发射 N 发子弹
- `追踪弹` — 子弹朝玩家当前位置飞行
- `激光扫射` — 预瞄线 + 瞬时伤害
- `旋转弹幕环` — 子弹围成圆环旋转扩散
- `分身` — 分裂出 2 个镜像
- `召唤小兵` — 在屏幕边缘生成普通敌人

### 5.5 Collision.js — 碰撞检测

**策略：** 矩形碰撞检测（AABB）

```javascript
function checkCollision(a, b) {
    return a.x < b.x + b.width &&
           a.x + a.width > b.x &&
           a.y < b.y + b.height &&
           a.y + a.height > b.y;
}
```

**碰撞分组：**
| 组 A | 组 B | 结果 |
|------|------|------|
| 玩家子弹 | 敌人 / Boss | 扣血、加分、粒子特效 |
| 敌人子弹 | 玩家 | 玩家扣血、降级 |
| 敌人 | 玩家 | 玩家扣血、双方碰撞 |
| 玩家 | 道具 | 拾取道具 |

### 5.6 Wave.js — 波次生成系统

**配置化波次定义：**

```javascript
const WAVE_CONFIG = {
    1: { enemies: [{ type: 'SCOUT', count: 5, interval: 800 }] },
    2: { enemies: [{ type: 'SCOUT', count: 3 }, { type: 'ASSAULT', count: 2 }] },
    3: { enemies: [{ type: 'ASSAULT', count: 4 }, { type: 'SNIPER', count: 1 }] },
    4: { enemies: [{ type: 'ASSAULT', count: 2 }, { type: 'SUICIDE', count: 3 }] },
    5: { boss: 'BOSS_1', enemies: [{ type: 'SCOUT', count: 2 }] },
    // ... 后续波次
};
```

**难度递增规则：**
- 每 5 波出现一个 Boss
- 普通波次中，敌人数量、速度、攻击频率递增
- 第 10 波后出现精英怪
- 第 15 波为最终 Boss

### 5.7 Particle.js — 粒子特效系统

**特效类型：**
- `爆炸碎片` — 敌人被击毁时的碎片飞散
- `引擎尾焰` — 玩家战机尾部的火焰效果
- `子弹轨迹` — 子弹飞行的拖尾光效
- `道具拾取` — 拾取道具时的光晕
- `Boss 登场` — Boss 出场震动特效
- `连击提示` — 连击数字弹出的动画

**粒子池设计：** 对象池模式，避免频繁 GC

```javascript
class ParticlePool {
    constructor(size) {
        this.pool = new Array(size).fill(null).map(() => new Particle());
        this.index = 0;
    }
    get(x, y, config) { /* 从池中取出粒子并初始化 */ }
    release(particle) { /* 重置粒子 */ }
}
```

### 5.8 Input.js — 输入系统

**双输入支持：**

```javascript
class InputSystem {
    constructor() {
        this.keys = {};        // 键盘按键状态
        this.touch = null;     // 触屏状态
        this.bindKeyboard();
        this.bindTouch();
    }

    // 获取移动方向
    getDirection() {
        if (this.touch) return this.touch.direction;
        return { x: this.keys.ArrowRight - this.keys.ArrowLeft,
                 y: this.keys.ArrowDown - this.keys.ArrowUp };
    }

    isShooting() { /* 自动射击 or 按住射击 */ }
    isSkillUsed() { /* 技能按键 */ }
}
```

**键盘映射：**
| 按键 | 功能 |
|------|------|
| ← → ↑ ↓ | 移动 |
| Z / Space | 射击（自动射击时无需按） |
| X | 释放技能 |
| B | 全屏炸弹 |
| Esc / P | 暂停 |
| Enter | 确认/开始 |

**触屏控制：**
- 左侧区域：手指滑动移动战机
- 右侧区域：点击释放技能 / 炸弹
- 自动射击（常开）

### 5.9 Audio.js — 音效系统

**方案：** Web Audio API + AudioContext

```javascript
class AudioSystem {
    constructor() {
        this.context = new AudioContext();
        this.sounds = new Map();
    }

    // 程序化生成音效（无需音频文件）
    generateShootSound() { /* 短促高频 */ }
    generateExplosionSound() { /* 低频爆破 */ }
    generatePowerUpSound() { /* 上升音阶 */ }
    generateBossAlert() { /* 低沉警报 */ }
    generateBGM() { /* 循环背景音乐 - 简单旋律 */ }
}
```

**说明：** 优先使用 Web Audio API 程序化生成音效，避免加载外部音频文件。如需更高质量音效，可替换为预录音频文件。

---

## 6. UI/UX 与视觉风格

### 6.1 视觉风格定义

| 维度 | 描述 |
|------|------|
| **主题** | 深空科幻、霓虹科技 |
| **主色调** | 深蓝 `#0a0e27`、暗紫 `#1a1040` |
| **强调色** | 霓虹蓝 `#00f0ff`、电离紫 `#b000ff`、炽热橙 `#ff6a00` |
| **字体** | 等宽科技字体（Google Fonts: Orbitron / Rajdhani） |
| **UI 风格** | 毛玻璃效果（Glassmorphism）+ 霓虹边框 |
| **动效** | 流畅过渡、粒子特效、光晕脉冲 |

### 6.2 页面布局

```
┌──────────────────────────────────────────────┐
│  [主菜单]                                      │
│  ┌──────────────────────────────────────────┐ │
│  │          ╔══════════════════╗             │ │
│  │          ║  星际战机 ·  DELTA  ║             │ │
│  │          ╚══════════════════╝             │ │
│  │                                          │ │
│  │        [ 开始游戏 ]  [ 战机选择 ]          │ │
│  │        [  排行榜  ]  [  设置  ]           │ │
│  │                                          │ │
│  │  ── 最佳战绩: 99999 ──                   │ │
│  └──────────────────────────────────────────┘ │
│  [背景：动态星空粒子 + 光晕]                    │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│  [游戏画面]                                    │
│  ┌──────────────────────────────────────────┐ │
│  │  SCORE: 12450   LV: 3   COMBO: x2.0      │ │
│  │  ████████░░░░░░░░  HP                    │ │
│  │                                          │ │
│  │        [ 游戏区域 - 动态星空背景 ]          │ │
│  │        · 敌人从上方出现                    │ │
│  │        · 弹幕效果                         │ │
│  │        · 粒子特效                         │ │
│  │        · Boss 战                         │ │
│  │                                          │ │
│  │              ▲ 玩家战机 ▲                   │ │
│  │  [武器: ████░░]  [技能: 就绪]  [💣 x2]    │ │
│  └──────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

### 6.3 视觉特效清单

| 特效 | 实现方式 | 说明 |
|------|----------|------|
| 动态星空背景 | Canvas 绘制多层星星 | 视差滚动，远处慢、近处快 |
| 战机引擎尾焰 | 粒子系统 | 颜色渐变，大小随机 |
| 子弹拖尾 | 渐变透明线条 | 从亮到暗，长度递减 |
| 爆炸特效 | 粒子系统 + 闪光 | 碎片飞散 + 中心闪光 |
| 霓虹边框 | CSS box-shadow glow | 关键帧动画呼吸效果 |
| 光晕/辉光 | Canvas globalCompositeOperation | 叠加混合模式 |
| 屏幕震动 | Canvas 偏移 | Boss 攻击、爆炸时触发 |
| 连击提示 | 放大 + 渐隐动画 | 大号字体弹出 |
| 弹幕预警线 | 半透明线条 | 提前显示弹幕轨迹 |
| Boss 血量条 | 顶部专用 UI | 渐变色，分阶段标记 |

### 6.4 响应式适配

| 设备 | 适配策略 |
|------|----------|
| 桌面端 (≥1024px) | 全尺寸 Canvas，键盘操作 |
| 平板 (768-1023px) | 自适应 Canvas，触屏 + 键盘 |
| 手机 (<768px) | 全屏 Canvas，触屏控制，UI 间距调整 |

**核心适配逻辑：** Canvas 保持宽高比（16:9），根据屏幕大小缩放，UI 元素使用相对定位。

---

## 7. 数据与状态管理

### 7.1 游戏状态机

```
        ┌──────────┐
        │  MENU    │ ← ─ ─ ─ ─ ─ ─ ─ ─ ┐
        └────┬─────┘                     │
             │ 点击"开始游戏"              │
             ▼                            │
        ┌──────────┐                     │
        │  SELECT  │                     │
        └────┬─────┘                     │
             │ 选择战机                    │
             ▼                            │
        ┌──────────┐                     │
   ┌───▶│  PLAYING │──── 暂停 ────┐      │
   │    └────┬─────┘              │      │
   │         │ 死亡                ▼      │
   │         ▼              ┌──────────┐  │
   │    ┌──────────┐        │  PAUSE   │  │
   │    │GAME OVER │        └────┬─────┘  │
   │    └────┬─────┘             │ 继续    │
   │         │ 返回菜单           ▼        │
   │         └────────────────→ PLAYING ──┘
   └──────────────────────────────────┘
```

### 7.2 数据存储

**localStorage 存储内容：**

```javascript
const SAVE_DATA = {
    version: '1.0',
    highScore: 0,
    totalKills: 0,
    gamesPlayed: 0,
    unlockedShips: ['ship_1'],  // 默认解锁先锋号
    settings: {
        bgmVolume: 0.7,
        sfxVolume: 1.0,
        autoShoot: true,
        vibration: true
    }
};
```

### 7.3 全局常量

```javascript
// constants.js
const CANVAS = { WIDTH: 480, HEIGHT: 720 };  // 逻辑分辨率
const PLAYER = { SPEED: 5, FIRE_RATE: 8 };
const BULLET = { SPEED: 10, DAMAGE: 1 };
const COLORS = {
    NEON_BLUE: '#00f0ff',
    NEON_PURPLE: '#b000ff',
    NEON_ORANGE: '#ff6a00',
    DEEP_BLUE: '#0a0e27',
    DARK_PURPLE: '#1a1040'
};
const WAVE = { BOSS_INTERVAL: 5, MAX_BOSS_WAVE: 15 };
```

---

## 8. 资源清单

### 8.1 代码资源（需手动编写）

| 文件 | 预估行数 | 难度 | 说明 |
|------|----------|------|------|
| index.html | 50 | 低 | 页面骨架 |
| style.css | 300 | 低 | 全局样式、菜单、HUD 样式 |
| js/main.js | 30 | 低 | 入口文件 |
| js/Game.js | 200 | 高 | 游戏核心引擎 |
| js/Scene.js | 100 | 中 | 场景管理器 |
| js/entities/Player.js | 200 | 高 | 玩家逻辑 |
| js/entities/Enemy.js | 250 | 高 | 敌人逻辑 |
| js/entities/Boss.js | 300 | 高 | Boss 逻辑 |
| js/entities/Bullet.js | 80 | 中 | 子弹逻辑 |
| js/entities/PowerUp.js | 60 | 中 | 道具逻辑 |
| js/systems/Input.js | 150 | 中 | 输入系统 |
| js/systems/Collision.js | 80 | 中 | 碰撞检测 |
| js/systems/Particle.js | 150 | 中 | 粒子系统 |
| js/systems/Wave.js | 120 | 中 | 波次生成 |
| js/systems/Audio.js | 100 | 中 | 音效系统 |
| js/ui/HUD.js | 100 | 中 | 游戏内 HUD |
| js/ui/Menu.js | 150 | 中 | 菜单界面 |
| js/ui/Score.js | 60 | 低 | 分数显示 |
| js/utils/constants.js | 60 | 低 | 常量定义 |
| js/utils/math.js | 30 | 低 | 数学工具 |
| js/utils/storage.js | 40 | 低 | 本地存储 |
| **合计** | **~2600** | | |

### 8.2 图形资源（可 Canvas 绘制 / 可选图片）

| 资源 | 方案 | 说明 |
|------|------|------|
| 玩家战机 | Canvas 几何绘制 | 用 Path2D 绘制三角形+梯形组合 |
| 敌人 | Canvas 几何绘制 | 不同颜色和形状的几何体 |
| Boss | Canvas 几何绘制 | 大型多边形组合 |
| 子弹 | Canvas 绘制 | 圆形/矩形 + 发光效果 |
| 道具 | Canvas 绘制 | 图标 + 旋转动画 |
| 背景 | Canvas 绘制 | 动态星空粒子 |
| 特效 | Canvas 粒子系统 | 全部程序化生成 |

**说明：** 所有图形资源均可通过 Canvas API 程序化绘制，无需外部图片文件。如需更高质量，可替换为 PNG/SVG 图片资源。

### 8.3 音频资源（可选）

| 音效 | 方案 | 说明 |
|------|------|------|
| 射击音效 | Web Audio API 生成 | 短促高频噪音 |
| 爆炸音效 | Web Audio API 生成 | 低频噪音衰减 |
| 拾取音效 | Web Audio API 生成 | 上升音阶 |
| Boss 警报 | Web Audio API 生成 | 低频脉冲 |
| 背景音乐 | Web Audio API 生成 | 简单循环旋律 |

---

## 9. 开发路线图

### 第一阶段：基础框架（预计 2-3 天）

- [ ] 搭建项目目录结构
- [ ] 实现 Canvas 初始化与游戏主循环
- [ ] 实现输入系统（键盘 + 触屏）
- [ ] 实现场景管理器和菜单场景
- [ ] 绘制动态星空背景

### 第二阶段：核心玩法（预计 3-4 天）

- [ ] 实现玩家战机（移动、射击、碰撞）
- [ ] 实现基础敌人（3 种类型）
- [ ] 实现子弹系统
- [ ] 实现碰撞检测系统
- [ ] 实现波次生成系统
- [ ] 实现 HUD 显示（分数、血量、武器等级）

### 第三阶段：丰富内容（预计 3-4 天）

- [ ] 实现武器升级系统（5 级）
- [ ] 实现道具系统（5 种道具）
- [ ] 实现更多敌人类型（6 种）
- [ ] 实现粒子特效系统（爆炸、尾焰、拖尾）
- [ ] 实现连击评分系统
- [ ] 实现游戏结束与结算界面

### 第四阶段：Boss 战（预计 2-3 天）

- [ ] 实现 Boss 基类与阶段转换
- [ ] 实现 Boss 1（巨像级）
- [ ] 实现 Boss 2（毁灭级）
- [ ] 实现最终 Boss（3 形态）
- [ ] 实现 Boss 出场特效
- [ ] 实现 Boss Rush 模式

### 第五阶段：打磨优化（预计 2-3 天）

- [ ] 实现音效系统（Web Audio API）
- [ ] 实现战机选择界面
- [ ] 实现排行榜（localStorage）
- [ ] 优化移动端触屏体验
- [ ] 性能优化（对象池、减少绘制调用）
- [ ] 添加屏幕震动、连击提示等视觉反馈
- [ ] 响应式适配测试

### 第六阶段：发布（预计 1 天）

- [ ] 代码优化与重构
- [ ] 平衡性调整（敌人血量、掉落率、难度曲线）
- [ ] 兼容性测试（Chrome、Safari、Edge）
- [ ] 移动端测试
- [ ] 部署到静态托管（Vercel / GitHub Pages）

---

## 10. 附录：参考与灵感来源

### 参考游戏

| 游戏 | 参考要素 |
|------|----------|
| 雷霆战机 | 武器升级系统、战机选择 |
| 傲气雄鹰（Sky Force） | 弹幕设计、Boss 战 |
| 怒首领蜂 | 弹幕难度、连击系统 |
| 微信小游戏《飞机大战》 | 操作手感、简洁入口 |

### 推荐学习资源

- [MDN Canvas 教程](https://developer.mozilla.org/zh-CN/docs/Web/API/Canvas_API/Tutorial)
- [Game Development MDN](https://developer.mozilla.org/zh-CN/docs/Games)
- [requestAnimationFrame 详解](https://developer.mozilla.org/zh-CN/docs/Web/API/Window/requestAnimationFrame)

---

> **文档版本记录**  
> v1.0 (2026-07-27) — 初始版本，完整技术设计文档